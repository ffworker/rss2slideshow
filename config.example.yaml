# stuff about the kiosk itself, NOT its feed list.
# all rss sources go into content/feeds.txt (copy the example first)
brand_name: rss2slideshow
accent_color: "#8b3932"

max_articles: 5     # per feed
refresh_minutes: 10

# only enable after checking the feeds' image usage rights for your display
news_images: false
