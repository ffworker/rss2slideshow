# settings for the kiosk itself. feeds are in content/feeds.txt,
# customer's colors/name/font are in branding/brand.yaml.

max_articles: 5     # per feed
refresh_minutes: 10

# only enable after checking the feeds' image usage rights for your display
news_images: false
